Task 1: Calculate Factorial Using a Function
Functionality:

>>The program prompts the user to enter a number.

>>It defines a function named factorial(n) that calculates the factorial of that number using a loop or recursion.

>>The function:

>>Multiplies all whole numbers from 1 up to n.

>>Returns the result.

>>The program calls the function with the user’s input and prints the calculated factorial.

Example:

>>Input: 5

>>Output: Factorial of 5 is: 120